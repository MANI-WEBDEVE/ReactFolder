#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir//ust/bin/env" ]; then
  exec "$basedir//ust/bin/env" node "$basedir/../node-js-simple-calculator/index.js" "$@"
else 
  exec /ust/bin/env node "$basedir/../node-js-simple-calculator/index.js" "$@"
fi
